{
  "name": "device-ingestion-testing",
  "version": "1.0.0",
  "description": "Direct HTTP, WebSocket, and TCP ingestion server for health watches.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.17.1"
  },
  "engines": {
    "node": ">=18"
  }
}
